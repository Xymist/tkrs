---
id: t-569e
status: open
deps: []
links: []
created: 2026-01-27T21:32:12.891142316Z
type: task
priority: 2
assignee: Xymist
---
# Validate parent ID on create

create --parent should resolve partial ID and fail when not found

## Implementation Plan

- In `cmd_create`, resolve the `--parent` value via existing partial-ID resolution (using the loaded tickets) before writing the file; propagate not-found/ambiguous errors.
- Ensure the resolved parent ID is written to frontmatter (not the raw input) and skipped only when no parent provided.
- Add tests: valid exact ID, valid partial, ambiguous parent error, missing parent error.

## Acceptance Criteria

- `tk create --parent <partial>` resolves to an existing ticket ID; ambiguous or missing IDs produce errors and do not create a ticket file.
- Parent frontmatter stores the resolved full ID.
- Tests cover happy path and error cases and pass.
