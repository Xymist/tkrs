---
id: t-75e3
status: open
deps: []
links: []
created: 2026-01-27T21:32:10.754378286Z
type: task
priority: 2
assignee: Xymist
---
# Fix undep partial-id resolution

undep should resolve both ticket and dependency IDs with partial matches and validate dependency exists

## Implementation Plan

- Update `cmd_undep` to resolve both the primary ticket ID and dependency ID using `resolve_partial_id`/`resolve_ticket_path` (or shared helper) to match partials and surface ambiguity/not-found errors.
- Ensure the resolved dependency is present; if absent, return a clear error instead of silent success.
- Add tests covering: successful partial matches for both args, ambiguous IDs, missing IDs, and removal of an existing dependency.

## Acceptance Criteria

- `tk undep <partial-ticket> <partial-dep>` resolves partial IDs consistently with other commands and fails on ambiguous/missing IDs.
- Attempting to undep a dependency not present returns an error message (not silent success).
- Tests demonstrate the above behaviors and pass.
