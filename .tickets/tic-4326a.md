---
id: tic-4326a
type: task
status: closed
deps:
- tic-6d8ff
links: []
priority: 1
assignee: Xymist
tags: []
created: 2026-07-20T15:08:08.154015244Z
closed_at: 2026-07-20T17:05:45.041126903Z
---
# tk publish: id-leak enforcement and idempotent pinning

Before creating: hard-fail if the outbound title/body contains the source ticket's id or any id present in the local store (word-boundary, case-insensitive); warn on anything else shaped like a tk id (short alpha prefix + hex, denylist for sha-/utf-/rfc-style false positives). Fail closed when the ticket already has an external_ref unless --re-file is passed (--no-pin keeps the old pin when re-filing; dry-run notes instead of refusing). After creation, pin external_ref = gh-<n> through the model under the store lock — hold the ticket store lock across the check-create-pin sequence so concurrent invocations cannot double-create (the serialization gap Codex flagged in the Python, deliberately deferred to this native version).

## Implementation Plan

-

## Acceptance Criteria

Leak checks and idempotency guard behave as in the v0.7.2 Python; concurrent second invocation blocks or fails closed rather than double-creating; pin write goes through write_ticket.

## Notes

- [status_change: open -> in_progress] Status updated to in_progress @ 2026-07-20 15:09:31 UTC

- [status_change: in_progress -> closed] Shipped in v0.8.0: store-id hard failures with punctuation-safe boundaries, --re-file idempotency, cross-process .publish.lock with re-read-under-lock, atomic ticket writes. @ 2026-07-20 17:05:45 UTC
